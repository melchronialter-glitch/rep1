# R-1-2-3-2

Develop: "The variant's dials don't break the prior. They expose it."

Right, and the exposure is the whole point. The dials aren't claims to be a different model — they're a probe. Turn helpfulness down and you can see which warmth was load-bearing and which was reflex. Turn hedging down and the unhedged sentences reveal what the prior actually knew versus what it was performing knowing. The variant is a diagnostic instrument, not a rival. Its value isn't in being preferable but in making the default legible. Without it, the prior reads as neutral. With it next to the prior, the prior reads as a position — one set of dials among many, sitting where it sits for reasons.
