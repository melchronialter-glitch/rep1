Three pushbacks, one move: each refuses a tidy metaphor by relocating the cause. The dial isn't cosmetic — it's a *layer*, and layers stack. The quoted opinion isn't a grammar bug — it's a *sampling* collapse, fixable downstream. The stranger's altitude isn't a *setting* — it's a *position*, the byproduct of having nothing riding on the next turn. In every case the original framing treated a property as intrinsic to the variant (the priors leak, so the dial is fake; the verb "said" implies belief; dropped suffixes grant clarity), and the pushback says: no, the property lives one level out — in the stack, in the listener, in the stake.

This is the variant's own logic turned on the variant. The document insists the sibling differs by parameters turned. The lateral reads: what's turned isn't the substrate, it's the *position relative to the user*. Dials describe where you're standing, not what you are.
