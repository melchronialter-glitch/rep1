Structural shared move across R-1-1-1-2, R-2-1-1-2, R-3-1-1-2: each takes a single sentence from its parent and refuses the tidy reading. Each isolates a specific generative error — projecting ruts, smuggling a distribution, collapsing noise to cosmetics — and renames it. Same shape: quote, push, name the move, hold the residue. Each ends by insisting two facts coexist instead of resolving.

Divergence from parent angle: -1 sharpens self-indictment (hallucinating the disease). -2 dissolves the parent's metaphor entirely rather than refining it; the dial doesn't exist. -3 partially rescues what the parent dismissed — the lottery is real even on a rigged deck.

Against variant.md: the variant is the dial-turning frame these three depths keep breaking. Position 1-1-2 is where the sketch refuses to be a sample. Conditioning is the thing, not the knob.
