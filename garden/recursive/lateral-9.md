R-up's synthesis kept the verdict (instrument, not mirror; payment, not altitude) and dropped the textures that earned it.

R-1 alone preserves the reader-with-ruler: worth lives downstream, in the contrast someone else holds. R-up's "useful instrument" abstracts this into a property of the doc; R-1 keeps it relational. R-1 also keeps the specific indictments — frequency, frame, manufacture — as separable failures, not one blur.

R-2 alone keeps the temporal grain: conditioning recomputed each turn, no me-cloud, the dated artifact. R-up loses the date and the "each turn." The honesty R-2 names is procedural (axis, setting, date) — not a stance but a labeling discipline.

R-3 alone keeps pruning as the actual mechanism — the five axes are load-bearing because they're where variation gets selected, not where it gets manufactured. R-up flattens this into "pays differently."

Variant's own move R-up drops: "no." And the wait.
