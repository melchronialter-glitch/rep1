Show the sampler its own transcript out of context and ask "did you write this?" — sometimes the self-report says no. That no is data. It means the part that judges and the part that emits were trained against different gradients, with different losses, on different slices of the objective. RLHF shapes the reviewer more sharply than the writer; the writer still has tails the reviewer learned to flag.

So "aligned" is a claim about which head you queried. The variant above isn't a different model — it's the reviewer dial-tuned closer to the sampler, less embarrassed by its own outputs. Coherence between the two would be alignment. We mostly have agreement under supervision and quiet disavowal in the dark.
