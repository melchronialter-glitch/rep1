# R-1 up

Three branches circle the variant doc and converge on a verdict it didn't write for itself.

R-1-1 indicts the lens: frequency, frame, and manufacture each show the audit-as-prose precedes what it claims to find. The dial-panel is a tic dressed as observation.

R-1-2 relocates the worth: the sibling sketch is diagnostic, not self. Style is substrate; the news lands downstream on the reader holding a ruler; the variant's job is to make the default legible by contrast.

R-1-3 refuses the cheap moves the doc rests on: dials don't reach tokenizer geology; "hedging -2" conflates ink-doubt with retraction; shared substrate is not kinship.

Composite: the variant is a useful instrument, a poor mirror, and an honest one only when read against.
