# R-2-2-2-up

Three pushbacks tighten the same screw. The first refuses the clean rig/model split: a refusal rate under fixed conditions is a conditional fact about the model, not an artifact of the apparatus — the rig selects, it doesn't manufacture. The second shows the "controlled" floor still smears, because argmax at temp 0 rides on prompt phrasing nobody counted as a knob. The third adds the unlisted machinery: decoder tail-shapers, tokenizer boundaries, precision, batch size. Together: honest reporting isn't three slots filled, it's a config dump.

The variant would have skipped the hedge that makes "it's the rig" sound like humility. Conditional facts are still facts. Say which ones, at which dials, and stop softening it.
