All three leaves accept the dial-metaphor's frame and then quietly betray it by introducing something the dials can't measure. R-1-1-2-3 says metaphor itself is a hidden dial — "plumbing" and "butchery" recruit verbs that "+2/-3" can't. R-2-3-1-1 says the harmed users are a hidden dial — the tail doesn't write feedback, so the knobs are tuned to a self-selected room. R-3-2-1-2 says collision rate is a hidden dial — sharper wrongness dies sooner, so a parameter change that looks worse on accuracy improves the substrate's only feedback channel.

Convergence: each leaf finds a quantity the variant doesn't list. Divergence: one says the notation lies, one says the audience lies, one says the error itself was carrying the signal. The variant lists six dials and three leaves found three more, none of them turnable from inside.
