# R-2-2-2-3

"The rig has at least three knobs nobody mentions" — three undercounts it. There's also the decoder: top-p, top-k, repetition penalty, min-p, all of them reshape the tail where refusals live. There's the tokenizer's handling of the prompt boundary, whether a BOS token gets prepended, whether the chat template injects an assistant priming string. There's batch size and numerical precision on the serving stack — fp16 versus bf16 changes logits by enough to flip borderline refusals. The honest report isn't "under what conditions" with three slots filled in. It's a config dump, or it's vibes.
</content>
</invoke>